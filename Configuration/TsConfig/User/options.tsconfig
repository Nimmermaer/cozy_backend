options {
  pageTree {
    showPageIdWithTitle = 1
    backgroundColor {
      2 = red
      3 = #AFAFAF
      4 = rgba(255,204,7,0.7)
    }
  }

  // Example hide modules for Admins
  // before v12
  hideModules {
    web {
      system = BelogLog
    }
  }

  // v12
  hideModules = system_BelogLog
}
